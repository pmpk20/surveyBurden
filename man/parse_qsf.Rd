% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_qsf.R
\name{parse_qsf}
\alias{parse_qsf}
\title{Parse a Qualtrics \code{.qsf} into a standardised question catalogue}
\usage{
parse_qsf(x)
}
\arguments{
\item{x}{A path to a \code{.qsf} file, or a \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}
}
\value{
A \link[tibble:tibble]{tibble}, one row per live question, ordered by flow
position then within-block position. Columns:
\code{question_id}, \code{flow_order}, \code{block_id}, \code{block_name}, \code{std_type},
\code{qualtrics_type}, \code{selector}, \code{subselector}, \code{n_options}, \code{n_rows},
\code{n_cols}, \code{text_words}, \code{max_label_words}, \code{label_text} (response/answer
option labels, lowercased and joined; \code{NA} if none), \code{options_numeric}
(\code{TRUE} if every response option label is a number), \code{question_text}
(HTML-stripped, truncated to 200 characters), \code{is_hidden} (\code{TRUE} if the
question is hidden from the respondent via injected CSS/JS),
\code{has_display_logic}, \code{display_logic_refs}, \code{has_validation}, \code{in_loop},
\code{loop_max}, \code{flag}.
}
\description{
The Layer 1 entry point. Reads the file (or an already-loaded object),
resolves the flow-reachable blocks, and classifies every live question into a
standardised type with the structural fields the burden scorer needs.
}
\details{
This function does not resolve respondent paths or compute burden. It reads
and classifies questions.
}
