% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_catalogue.R
\name{validate_catalogue}
\alias{validate_catalogue}
\title{Validate (and coerce) a question catalogue}
\usage{
validate_catalogue(catalogue)
}
\arguments{
\item{catalogue}{A data frame (or tibble) with at least the columns
\code{question_id} and \code{std_type}. See \strong{Details} for the full schema.}
}
\value{
A tibble with all required, structural, and platform-specific columns
present (missing optional columns filled with their defaults).
}
\description{
Checks that a data frame has the columns \code{\link[=score_burden]{score_burden()}} needs and coerces
it to a tibble.
}
\details{
A catalogue from \code{\link[=parse_qsf]{parse_qsf()}} always passes. This function is useful when
you construct a catalogue by hand for a non-Qualtrics survey: it confirms the
required columns are present, fills optional columns with safe defaults, and
converts the result to a tibble so downstream functions work unchanged.

\strong{Required columns} (must be present):
\describe{
\item{\code{question_id}}{Character. Unique identifier for each question.}
\item{\code{std_type}}{Character. Standardised question type: one of
\code{"single_choice"}, \code{"multi_choice"}, \code{"matrix"}, \code{"open_text"},
\code{"slider"}, \code{"ranking"}, \code{"constant_sum"}, \code{"descriptive"}, \code{"timing"},
\code{"meta"}, \code{"captcha"}.}
}

\strong{Structural columns} (used by the scorer; default to safe values if
missing):
\describe{
\item{\code{n_options}}{Integer. Number of response options (\code{NA} if unknown).}
\item{\code{n_rows}}{Integer. Number of matrix rows (\code{NA} for non-matrix types).}
\item{\code{n_cols}}{Integer. Number of matrix columns (\code{NA} for non-matrix types).}
\item{\code{text_words}}{Integer. Word count of the question stem.}
\item{\code{is_dropdown}}{Logical. \code{TRUE} if a single-choice dropdown.}
\item{\code{is_multiline}}{Logical. \code{TRUE} if a multi-line open-text field.}
\item{\code{is_multi_answer}}{Logical. \code{TRUE} if a checkbox-per-cell matrix.}
\item{\code{is_hidden}}{Logical. \code{TRUE} if hidden from the respondent.}
\item{\code{options_numeric}}{Logical. \code{TRUE} if all option labels are numbers.}
\item{\code{label_text}}{Character. Lowercased, joined response labels.}
\item{\code{question_text}}{Character. Plain-text question stem.}
\item{\code{has_display_logic}}{Logical. Whether the question has display logic.}
\item{\code{has_validation}}{Logical. Whether a response is forced.}
\item{\code{in_loop}}{Logical. Whether the question is inside a loop block.}
\item{\code{loop_max}}{Integer. Maximum loop iterations (\code{NA} if not in loop).}
\item{\code{flag}}{Character. Confidence flag (\code{"auto"}, \code{"inferred"},
\code{"manual"}, or \code{"unknown"}).}
}

\strong{Platform-specific columns} (retained for provenance; not used by the
scorer):
\describe{
\item{\code{qualtrics_type}}{Character. Original Qualtrics \code{QuestionType}.}
\item{\code{selector}}{Character. Qualtrics \code{Selector}.}
\item{\code{subselector}}{Character. Qualtrics \code{SubSelector}.}
}
}
\examples{
# Hand-craft a tiny catalogue
cat <- data.frame(
  question_id = c("Q1", "Q2", "Q3"),
  std_type    = c("single_choice", "open_text", "matrix"),
  n_options   = c(5L, NA, NA),
  n_rows      = c(NA, NA, 4L),
  n_cols      = c(NA, NA, 5L),
  stringsAsFactors = FALSE
)
validated <- validate_catalogue(cat)
validated

}
