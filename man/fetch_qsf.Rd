% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_qsf.R
\name{fetch_qsf}
\alias{fetch_qsf}
\title{Fetch a survey definition from the Qualtrics API}
\usage{
fetch_qsf(
  survey,
  api_key = Sys.getenv("QUALTRICS_API_KEY"),
  base_url = Sys.getenv("QUALTRICS_BASE_URL")
)
}
\arguments{
\item{survey}{Survey id (\code{SV_...}) or any Qualtrics URL containing one
(survey-builder link, distribution link, ...).}

\item{api_key}{Qualtrics API token. Defaults to \code{Sys.getenv("QUALTRICS_API_KEY")}.}

\item{base_url}{API base, e.g. \code{"https://fra1.qualtrics.com"}. Defaults to
\code{Sys.getenv("QUALTRICS_BASE_URL")}, else derived from \code{survey} if it is a URL.}
}
\value{
A \code{qsf_raw} object.
}
\description{
Downloads a survey's definition through the Qualtrics REST API and returns it
in the same \code{qsf_raw} shape \code{\link[=read_qsf]{read_qsf()}} produces from a \code{.qsf} file, so the
rest of the pipeline (\code{burden_report()} etc.) works unchanged.
}
\details{
Needs an API token. Set \code{QUALTRICS_API_KEY} (and optionally
\code{QUALTRICS_BASE_URL}, e.g. \code{"https://fra1.qualtrics.com"}) in your
environment, or pass them as arguments. The token needs the
"Manage Survey" / read-definition permission.
}
\examples{
\dontrun{
Sys.setenv(QUALTRICS_API_KEY = "...", QUALTRICS_BASE_URL = "https://fra1.qualtrics.com")
r <- burden_report(fetch_qsf("SV_0xIVRCCRgC1lt8a"))
}
}
