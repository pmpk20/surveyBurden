% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_qsf.R
\name{as_qsf_raw}
\alias{as_qsf_raw}
\title{Normalise a survey definition into the \code{qsf_raw} shape}
\usage{
as_qsf_raw(def)
}
\arguments{
\item{def}{A parsed survey definition list.}
}
\value{
A \code{qsf_raw} object.
}
\description{
Accepts either the \code{.qsf} file shape (\code{SurveyEntry} + \code{SurveyElements} array)
or the Qualtrics API \code{survey-definitions} shape (\code{Questions}/\code{Blocks}/
\code{SurveyFlow} objects) and returns the former.
}
\examples{
qsf_path <- system.file("extdata", "demo_travel_survey.qsf",
                         package = "surveyBurden")
raw_list <- jsonlite::fromJSON(qsf_path, simplifyVector = FALSE)
qsf <- as_qsf_raw(raw_list)
class(qsf)

}
