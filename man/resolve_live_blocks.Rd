% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_live_blocks.R
\name{resolve_live_blocks}
\alias{resolve_live_blocks}
\title{Resolve the survey blocks a respondent can actually reach}
\usage{
resolve_live_blocks(qsf)
}
\arguments{
\item{qsf}{A \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per live block:
\describe{
\item{flow_order}{Integer position in the flow (first occurrence).}
\item{block_id}{Qualtrics block id (\code{BL_...}).}
\item{block_name}{Block description.}
\item{in_loop}{\code{TRUE} if the block uses Loop & Merge.}
\item{loop_on_qid}{Question id the loop count depends on, else \code{NA}.}
\item{loop_max}{Maximum loop iterations, else \code{NA}.}
\item{question_ids}{List column: character vector of \code{QID}s in block order.}
}
}
\description{
Walks the \code{SurveyFlow} and returns the blocks it references, in flow order,
with their questions. Blocks that exist in the \code{.qsf} but are not shown by the
flow (typically the Qualtrics "Trash / Unused Questions" block) are excluded.
}
