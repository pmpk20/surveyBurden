% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_burden.R
\name{score_burden}
\alias{score_burden}
\title{Score per-question ex-ante burden}
\usage{
score_burden(catalogue, weights = gfs_weights())
}
\arguments{
\item{catalogue}{A tibble from \code{\link[=parse_qsf]{parse_qsf()}}.}

\item{weights}{A named list of weights; defaults to \code{\link[=gfs_weights]{gfs_weights()}}.}
}
\value{
\code{catalogue} with four columns added:
\describe{
\item{\code{gfs_points}}{Estimated GfS burden points (\code{NA} if the item cannot
be scored -- an unrecognised type).}
\item{\code{est_seconds}}{\code{gfs_points} converted at \code{points_per_minute}.}
\item{\code{score_flag}}{\code{"auto"} (the GfS mapping is deterministic from the
structure), \code{"inferred"} (the structure is clear but the mapping onto
a GfS category involved an explicit, documented judgement -- the number
is still determinate), \code{"manual"} (a human should check) or
\code{"unknown"} (unmapped type).}
\item{\code{score_basis}}{A short human-readable statement of how the number
was reached.}
}
}
\description{
Applies the GfS / Axhausen scoring rules (\code{\link[=gfs_weights]{gfs_weights()}}) to a question
catalogue from \code{\link[=parse_qsf]{parse_qsf()}}, adding GfS burden points, an estimated
completion time, a confidence flag and a plain-language rationale per item.
This is Layer 3 at the item level; propagation through respondent paths is a
later step.
}
\details{
The published GfS Table 1 point weights are applied directly where the
required structure can be read from the QSF. Where the QSF does not contain
the quantity GfS needs -- a dropdown has no Table 1 row, a slider is not in
the scheme, "lines" of text are not in a QSF -- the package applies an
explicit, documented inference rule. \code{score_flag} and \code{score_basis} say
which case each item is.
}
