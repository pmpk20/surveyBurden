% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_question.R
\name{classify_question}
\alias{classify_question}
\title{Classify a single Qualtrics question payload}
\usage{
classify_question(payload)
}
\arguments{
\item{payload}{The \code{Payload} list of one \code{SQ} survey element.}
}
\value{
A named \code{list} with elements \code{question_id}, \code{std_type},
\code{qualtrics_type}, \code{selector}, \code{subselector},
\code{is_dropdown} (\code{TRUE} for single-choice dropdowns),
\code{is_multiline} (\code{TRUE} for multi-line open text),
\code{is_multi_answer} (\code{TRUE} for checkbox-per-cell matrices),
\code{n_options}, \code{n_rows},
\code{n_cols}, \code{text_words}, \code{max_label_words} (longest response option / matrix
label, in words; \code{NA} if the question has no labels), \code{label_text} (all
response/answer labels lowercased and joined with \code{" | "}, truncated; \code{NA}
if none), \code{options_numeric} (\code{TRUE} if the question has >= 3 response
options and every one is a number), \code{question_text} (HTML-stripped,
truncated to 200 characters), \code{is_hidden} (\code{TRUE} if injected CSS/JS hides
the question from the respondent), \code{has_display_logic},
\code{display_logic_refs} (character vector), \code{has_validation} and \code{flag}.
\code{\link[=parse_qsf]{parse_qsf()}} assembles one tibble from these across every live question.
}
\description{
Maps a Qualtrics \code{QuestionType} / \code{Selector} / \code{SubSelector} combination to a
standardised question type and extracts the structural fields the burden
scorer needs. This function reads structure only, never semantics: a two-option
multiple choice is \code{single_choice} whether it means yes/no or something else.
}
\details{
\code{flag} is one of \code{"auto"} (confident structural mapping),
\code{"inferred"} (mapping needs an assumption, e.g. dropdown scored by option
count, short text that might be a code field), \code{"manual"} (reserved for cases
a human must check) or \code{"unknown"} (unmapped type).
}
\examples{
qsf_path <- system.file("extdata", "demo_travel_survey.qsf",
                         package = "surveyBurden")
qsf <- read_qsf(qsf_path)
questions <- Filter(function(el) identical(el$Element, "SQ"),
                    qsf$SurveyElements)
cls <- classify_question(questions[[1]]$Payload)
cls$std_type
cls$n_options

}
