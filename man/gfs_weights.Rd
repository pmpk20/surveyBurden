% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gfs_weights.R
\name{gfs_weights}
\alias{gfs_weights}
\title{GfS / Axhausen burden weights}
\usage{
gfs_weights()
}
\value{
A named list of weights.
}
\description{
The default scoring backend. Values are transcribed from Table 1 of
Heimgartner & Axhausen (2024), "Predicting Response Rates Once Again"
(\emph{Findings}), which prints the GfS / ETH Zurich scheme (GfS Zurich 2006,
updated). Override any element and pass the result to \code{\link[=score_burden]{score_burden()}}.
}
\details{
Table 1 items and the names used here:
\tabular{lll}{
GfS item \tab points \tab weight name \cr
Question or transition (up to 3 lines) \tab 2.0 \tab \code{descriptive_base} \cr
Each additional line \tab 1.0 \tab \code{descriptive_per_extra_line} \cr
Closed yes/no answers \tab 1.0 \tab \code{yes_no} \cr
Simple numerical answer (e.g. year of birth) \tab 1.0 \tab \code{numeric_answer} \cr
Rating with up to 5 possibilities \tab 2.0 \tab \code{rating_small} \cr
Rating with more than 5 possibilities \tab 3.0 \tab \code{rating_large} \cr
Best of ranking with cards \tab 4.0 \tab \code{rank_base} \cr
Second and each additional best ranking \tab 3.0 \tab \code{rank_per_item} \cr
Answer to sub-questions of up to 5 words \tab 1.0 \tab \code{open_short} \cr
Answer to sub-questions of up to 2 lines \tab 2.0 \tab \code{open_medium} \cr
Half-open, < 8 possibilities (+ each additional) \tab 2.0 (+2.0) \tab \code{multi_small_base} / \code{multi_small_per_option} \cr
Half-open, >= 8 possibilities (+ each additional) \tab 4.0 (+3.0) \tab \code{multi_large_base} / \code{multi_large_per_option} \cr
Answer to "please specify" \tab 2.0 \tab \code{please_specify} \cr
First answer to an open question \tab 6.0 \tab \code{open_essay} \cr
Each additional answer to the open question \tab 3.0 \tab \code{open_essay_per_extra} \cr
Filter \tab 0.5 \tab \code{filter} \cr
Branching \tab 0.5 \tab \code{branching} \cr
Stated choice question, 2 alternatives \tab 2.0 \tab \code{sc_2_alt} \cr
Stated choice question, 3 alternatives \tab 3.0 \tab \code{sc_3_alt} \cr
Per SC variable, per question \tab 1.0 \tab \code{sc_per_variable} \cr
}

Not in Table 1 -- package-derived, used only where the QSF does not contain
the quantity the scheme needs:
\describe{
\item{\code{slider}}{2.0 -- GfS has no slider; a graded slider is mapped to a
rating, a magnitude slider to a numerical answer (see \code{\link[=score_burden]{score_burden()}}).}
\item{\code{matrix_cell_multi}}{0.5 -- a multiple-answer ("tick all that apply")
grid has no Table 1 rule. Each of its \verb{rows x cols} cells is treated as
one trivial closed yes/no decision (GfS closed yes/no is 1.0), discounted
for grid working-memory efficiency. This is orientation-invariant -- the
score does not depend on which axis the QSF stored as rows. A documented
inference, not GfS; the vignette describes the alternatives that were
considered.}
\item{\code{points_per_minute}}{12 -- the published GfS rule of thumb ("twelve
points roughly correspond to a one-minute response time", Heimgartner &
Axhausen 2024). Overridable: set \code{w$points_per_minute} on the returned
list, or feed a survey-specific figure from \code{\link[=validate_times]{validate_times()}}.}
\item{\code{words_per_line}}{12 -- a \strong{pragmatic conversion heuristic}, not an
empirically calibrated constant. GfS scores instruction text in rendered
lines; a QSF has words, not a rendered width. Overridable, and exposed
directly as \code{burden_report(words_per_line = ...)}. The single most
sensitive assumption for \code{descriptive} items.}
}
}
