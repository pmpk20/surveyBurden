% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculation_certainty.R
\name{calculation_certainty}
\alias{calculation_certainty}
\title{Where the burden calculation is exact, and where it rests on assumptions}
\usage{
calculation_certainty(
  qsf,
  weights = gfs_weights(),
  max_paths = 10000L,
  paths = NULL,
  scored = NULL,
  blocks = NULL
)
}
\arguments{
\item{qsf}{A path to a \code{.qsf}, or a \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}

\item{weights}{A \code{\link[=gfs_weights]{gfs_weights()}} list.}

\item{max_paths}{Passed to \code{\link[=resolve_paths]{resolve_paths()}}.}

\item{paths, scored, blocks}{Optional precomputed \code{\link[=resolve_paths]{resolve_paths()}},
\code{\link[=score_burden]{score_burden()}} and \code{\link[=resolve_live_blocks]{resolve_live_blocks()}} results for this \code{qsf}
(internal reuse by \code{\link[=burden_report]{burden_report()}}; \code{NULL} computes them here, leaving
the public behaviour unchanged).}
}
\value{
An object of class \code{calculation_certainty} (list):
\describe{
\item{paths}{\code{n_full}, \code{n_exact} (no unresolved display-logic conditions),
\code{n_with_unresolved}, and a \code{per_path} tibble.}
\item{branches}{One row per flow \code{Branch}, with the trigger variable(s).
Both outcomes of every branch are always enumerated, so a branch is not
an approximation -- but which outcome a given respondent takes is
unknown ex ante.}
\item{display_logic}{\code{n_conditional} questions gated by an unresolved
condition on some full path; \code{n_exact} scored by exact joint
enumeration of their gate states; \code{n_approx} scored with the
primary-gate approximation; component counts; and
\code{cross_component_independence} (\code{TRUE} when >1 coupling component is
convolved as independent).}
\item{loops}{\code{n_known} loop blocks with an explicit iteration cap,
\code{n_unknown} with a dynamic/unknown bound, and a \code{bounds} tibble.}
\item{scores}{Counts of \code{score_flag}: \code{auto} (confident structural
mapping), \code{inferred} (mapping needs an assumption), \code{manual},
\code{unknown}.}
}
}
\description{
\code{burden_report()} propagates an established item score through a survey's
flow and display logic. Some of that propagation is exact (a fixed block
sequence, a loop with an explicit cap); some rests on documented assumptions
(the gate-independence approximation for large display-logic components, the
independence of separate coupling components). This function reports the
split so a reader can see which parts of the output to trust fully and which
to read as assumption-dependent.
}
\examples{
\dontrun{
calculation_certainty("survey.qsf")
}
}
