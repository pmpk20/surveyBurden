% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden_report.R
\name{burden_report}
\alias{burden_report}
\alias{summary.burden_report}
\title{Ex-ante instrument burden report}
\usage{
burden_report(
  x,
  weights = gfs_weights(),
  profile = TRUE,
  routes = NULL,
  rare_threshold = 1500,
  stem_warning_threshold = 40L,
  label_warning_threshold = 10L,
  words_per_line = NULL,
  certainty = TRUE,
  quiet = FALSE
)

\method{summary}{burden_report}(object, ...)
}
\arguments{
\item{x}{A path to a \code{.qsf} file, or a \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}

\item{weights}{A \code{\link[=gfs_weights]{gfs_weights()}} list.}

\item{profile}{If \code{TRUE} (default), enumerate the display-logic structural
burden profile (\code{\link[=path_burden_profile]{path_burden_profile()}}) to get the \emph{achievable} min/median/
max burden for this survey -- a few seconds of extra work. \code{FALSE} skips
that and falls back to a much faster but naive range (see Details): min/max
only, no median, and the numbers can understate the true minimum.}

\item{routes}{Optional respondent data frame (see \code{\link[=respondent_burden]{respondent_burden()}}).
When supplied, the report adds a population-weighted burden summary: each
route is weighted by how often respondents actually take it, so unlike the
structural profile this is a real average over respondents.}

\item{rare_threshold}{GfS points above which Heimgartner & Axhausen (2024)
found surveys to be rare (their sample: median 399, n = 79 waves). Used for
the "rare burden" warning and to scale the \code{index} column of \verb{$burden} to
0-1.}

\item{stem_warning_threshold}{Question stems longer than this many words are
flagged in the readability diagnostics (default 40). Diagnostic only --
this does \strong{not} change any GfS score.}

\item{label_warning_threshold}{For matrix/grid questions, response-option or
row labels longer than this many words are flagged (default 10). Diagnostic
only.}

\item{words_per_line}{Optional override for the descriptive-text "lines"
conversion (default 12, from \code{\link[=gfs_weights]{gfs_weights()}}). This is a pragmatic
conversion heuristic -- GfS scores instruction text in rendered lines, and
a QSF has words, not a rendered width -- not an empirically calibrated
constant. Set it to your survey theme's typical line length if you have
one.}

\item{certainty}{If \code{TRUE} (default), attach and print the
\code{\link[=calculation_certainty]{calculation_certainty()}} breakdown. \code{FALSE} skips it (a little faster).}

\item{quiet}{If \code{FALSE} (default), report progress through the pipeline
stages with \code{\link[cli:cli_progress_step]{cli::cli_progress_step()}}. \code{TRUE} silences it.}

\item{object}{A \code{burden_report} from \code{\link[=burden_report]{burden_report()}}.}

\item{...}{Not used.}
}
\value{
An object of class \code{burden_report}: a list of tibbles under stable
names. Print it for the formatted summary, or read its components:
\describe{
\item{instrument}{One-row tibble of structural counts: \code{survey_name},
\code{n_questions}, \code{n_blocks}, \code{n_branches}, \code{n_randomisers},
\code{n_loop_blocks}, \code{n_end_points}, \code{n_paths}, \code{n_complete_paths},
\code{n_screenout_paths}.}
\item{burden}{Five-row tibble, one row per statistic (\code{min}, \code{p25},
\code{median}, \code{p75}, \code{max}), with \code{points}, \code{minutes} (points per minute
from \code{\link[=gfs_weights]{gfs_weights()}}) and \code{index} (\code{points / rare_threshold}).
\code{attr(, "basis")} is \code{"structural"} when \code{profile = TRUE}, else
\code{"naive"} and only \code{min} / \code{max} are populated.}
\item{blocks}{One row per block, in survey order: \code{block_id},
\code{block_name}, \code{flow_order}, \code{n_questions}, \code{gfs_points} and \code{share}
(share of all-question points).}
\item{items}{The full \code{\link[=score_burden]{score_burden()}} table, every live question with
its \code{gfs_points}, \code{score_flag} and \code{score_basis}.}
\item{paths}{One row per structural path: \code{path_id}, \code{status} (factor
\code{complete} / \code{screen_out}), \code{n_blocks}, \code{n_questions_floor},
\code{n_questions_ceiling}, \code{burden_floor}, \code{burden_ceiling}, \code{n_gates}, and
\code{burden_min} / \code{burden_median} / \code{burden_max} (\code{NA} when
\code{profile = FALSE}).}
\item{readability}{Diagnostic, not part of the GfS score: \code{stem_threshold},
\code{label_threshold}, and the tibbles \code{long_stems}, \code{long_labels} and
\code{long_grids}.}
\item{certainty}{\code{\link[=calculation_certainty]{calculation_certainty()}} output, or \code{NULL} when
\code{certainty = FALSE}.}
\item{warnings}{Character vector of QC flags, in plain language.}
\item{population}{Present only when \code{routes} is supplied: a tibble the
same shape as \code{burden}, weighted by observed respondent routes.}
}
Scalars are attributes: \code{points_per_minute}, \code{rare_threshold}, \code{benchmark}
(\code{list(median_points, n_waves)}) and \code{profile_used}.
}
\description{
The user-facing entry point. Parses a Qualtrics \code{.qsf}, resolves its flow and
display logic, scores every question with the GfS / Axhausen scheme, and
summarises the burden across the instrument's structural path space.
}
\details{
\strong{Why two ranges exist.} \code{\link[=path_burden]{path_burden()}} (the fast, naive check) scores each
flow path by assuming every optional (display-logic-gated) question can be
independently hidden for the minimum, or independently shown for the maximum.
That assumption is not always achievable: some questions are gated by
conditions that are satisfied by default (e.g. "shown unless a specific answer
was picked"), so they cannot actually be hidden regardless of what else the
respondent answers. \code{\link[=path_burden_profile]{path_burden_profile()}} enumerates the real display-logic
combinations and finds the burden values that are genuinely reachable, which
is why its minimum is usually \emph{higher} than the naive floor. \strong{This report
uses the profile's range as the one range shown, whenever \code{profile = TRUE}.}

\strong{Path-space cap.} The structural path space is enumerated up to
\code{max_paths} (10000, set inside \code{\link[=resolve_flow]{resolve_flow()}}). A flow that would produce
more raises an error rather than a partial answer; a survey that hits it has
genuinely intractable routing. Block randomisers are treated as "all
sub-blocks shown, in survey order" -- the randomised subsets are not
enumerated.
}
\examples{
\dontrun{
br <- burden_report("survey.qsf")
br                       # formatted summary
summary(br)              # short headline
br$burden                # the min/median/max table
br$items[order(-br$items$gfs_points), ]   # questions by burden
br$paths
}
}
