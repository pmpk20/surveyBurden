% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_flow.R
\name{resolve_flow}
\alias{resolve_flow}
\title{Enumerate feasible respondent paths through the survey flow}
\usage{
resolve_flow(qsf, max_paths = 10000L)
}
\arguments{
\item{qsf}{A \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}

\item{max_paths}{Cap on the number of distinct paths. If the flow would
produce more, an error is raised rather than a partial result.}
}
\value{
A \link[tibble:tibble]{tibble}, one row per distinct path:
\describe{
\item{path_id}{Integer id.}
\item{block_ids}{List column: ordered \code{BL_...} ids the path shows.}
\item{terminates_early}{\code{TRUE} if the path hits an \code{EndSurvey} before the
end of the flow (a screen-out or quota termination).}
\item{decisions}{List column: named logical vector, one entry per branch
on a representative route to this path, \code{TRUE} = condition taken.}
}
}
\description{
Walks the \code{SurveyFlow}, forking at every \code{Branch} into a condition-true and a
condition-false path. Branch conditions are not evaluated: they depend on
embedded data or prior answers that are unknown ex ante, so both outcomes are
treated as feasible. The result is the \emph{structural path space} -- every block
sequence a respondent could encounter -- without any assumption about branch
probabilities.
}
\details{
Within-block display logic (whether an individual question is shown) is \emph{not}
resolved here; that is a separate step. Paths are at block granularity.
}
