% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path_burden.R
\name{path_burden}
\alias{path_burden}
\title{Burden per respondent path}
\usage{
path_burden(qsf, weights = gfs_weights(), max_paths = 10000L)
}
\arguments{
\item{qsf}{A \code{qsf_raw} object from \code{\link[=read_qsf]{read_qsf()}}.}

\item{weights}{A \code{\link[=gfs_weights]{gfs_weights()}} list.}

\item{max_paths}{Passed to \code{\link[=resolve_paths]{resolve_paths()}}.}
}
\value{
A \link[tibble:tibble]{tibble}, one row per flow path:
\code{path_id}, \code{terminates_early}, \code{n_blocks}, \code{n_q_floor}, \code{n_q_ceiling},
\code{gfs_floor}, \code{gfs_ceiling}, \code{min_minutes}, \code{max_minutes}, \code{n_gates}.
}
\description{
Joins \code{\link[=resolve_paths]{resolve_paths()}} to \code{\link[=score_burden]{score_burden()}} and reports, for every flow path,
a burden band: the \emph{floor} (only always-shown questions, loop blocks run once)
and the \emph{ceiling} (every reachable question, loop blocks run to their maximum).
The true burden of any actual respondent on that path lies between.
}
