% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/realised_burden.R
\name{realised_burden}
\alias{realised_burden}
\title{Realised response burden from observed responses}
\usage{
realised_burden(
  qsf,
  responses,
  scheme = gfs_scheme(),
  words_per_line = NULL,
  id_col = NULL
)
}
\arguments{
\item{qsf}{A \code{qsf_raw} object, a path to a \code{.qsf} file, or a survey id
accepted by \code{\link[=fetch_qsf]{fetch_qsf()}}.}

\item{responses}{A data frame of response data. Columns must be mappable to
question ids via one of these strategies (tried in order):
\enumerate{
\item A \code{col_map} attribute on the data frame: a named list, one entry per
column, each either a QID string or \code{list(qid = , iteration = )}. It
overrides the automatic strategies for the columns it names. With a
bare QID, the loop iteration comes from an \code{N_} prefix on the column
name (\verb{2_mycol} is iteration 2), else 0.
\item Column names match question ids directly (\code{QID15}, \code{QID15_1}, or
\verb{2_QID15} for loop iteration 2).
\item Column names match \code{DataExportTag} values from the QSF (\code{Q15},
\code{travel_mode_1}), exactly or ignoring case.
}

Leading label and ImportId rows from a raw Qualtrics CSV export are
removed, with a message, when recognisable (an ImportId JSON cell, or a
system column holding its own label such as \code{ResponseId} =
"Response ID"); other rows are always kept.

The recommended way to obtain this data frame is
\code{qualtRics::fetch_survey(survey_id, label = FALSE, convert = FALSE, add_column_map = FALSE)}.}

\item{scheme}{A \code{\link[=gfs_scheme]{gfs_scheme()}} list.}

\item{words_per_line}{How many words fit on one rendered line, used only when
scoring descriptive-text questions. \code{NULL} (default) uses
\code{scheme$words_per_line} (12). A single number applies to all respondents.
A numeric vector of length \code{nrow(responses)} gives per-respondent values,
allowing the user to reflect device differences (e.g., phone vs desktop).
The user is responsible for mapping device metadata to appropriate values;
the package does not assume any device-to-value mapping.}

\item{id_col}{Name of the respondent-id column. Auto-detected from
\code{"ResponseId"}, \code{"response_id"}, or the first column whose values are all
unique. Pass explicitly to override.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per respondent:
\describe{
\item{\code{response_id}}{Respondent identifier (from \code{id_col}).}
\item{\code{finished}}{Logical: did the respondent reach the end? Read from
a \code{Finished} column coded \code{1}/\code{0}, \code{TRUE}/\code{FALSE} or yes/no (any
case). \code{NA} means the status is unknown (no such column, a blank, or
an unrecognised value), not that the respondent did not finish.}
\item{\code{furthest_block}}{Integer ordinal of the last block in which the
respondent answered at least one question.}
\item{\code{n_questions_answered}}{Count of answered question-iteration
pairs: a non-loop question counts once, a looped question once per
iteration with at least one non-blank response column. Not a count of
distinct questions.}
\item{\code{realised_points}}{Sum of the full GfS+ scores of answered
questions, once per answered iteration for looped questions (see the
operational definition above).}
\item{\code{realised_minutes}}{\code{realised_points / points_per_minute}.}
\item{\code{predicted_points}}{Structural prediction for comparison, from
\code{\link[=respondent_burden]{respondent_burden()}}: the path matching the optional blocks the
respondent answered in, their loop counts inferred from the answered
iterations, and the path's model-weighted median display-logic
burden.}
\item{\code{predicted_minutes}}{\code{predicted_points / points_per_minute}.}
\item{\code{words_per_line}}{The words-per-line value used for each
respondent's descriptive-text scoring.}
\item{\code{n_unmapped_cols}}{Number of response columns that could not be
mapped to any question in the QSF.}
}
}
\description{
An answer-based GfS+ estimate for each respondent, from their response
data and the survey's structure in the QSF. Unlike \code{\link[=respondent_burden]{respondent_burden()}},
which predicts burden from structural paths, this function scores what
respondents answered.
}
\details{
\strong{Operational definition.} A question counts as answered when at least
one of its mapped response columns is non-blank, and then its full GfS+
score is added -- separately for each detected Loop & Merge iteration.
A partly answered matrix therefore scores in full. Realised burden does
not measure exposure (questions shown but left blank score 0), reading
time, or the share of a question completed.
}
\section{Recommended Qualtrics export}{

Via the API (cleanest):

\if{html}{\out{<div class="sourceCode">}}\preformatted{responses <- qualtRics::fetch_survey(
  surveyID   = "SV_...",
  label      = FALSE,    # numeric recode values, not choice text
  convert    = FALSE,    # keep raw strings, don't coerce
  force_request = TRUE   # bypass cache
)
qsf <- fetch_qsf("SV_...")
rb  <- realised_burden(qsf, responses)
}\if{html}{\out{</div>}}

Via CSV download: Data & Analysis > Export & Import > Export Data > CSV.
Tick "Use numeric values". Read with
\code{read.csv("file.csv", check.names = FALSE)} and pass directly. The column
names will be the question export tags; the function maps them via the QSF.
}

\examples{
\donttest{
qsf_path <- system.file("extdata", "demo_travel_survey.qsf",
                         package = "surveyBurden")
# Simulate response data with QID-based column names
responses <- data.frame(
  ResponseId = paste0("R_", 1:5),
  Finished   = c(1, 1, 1, 0, 0),
  QID1       = rep(NA, 5),          # descriptive text, no response
  QID2       = c("1", "2", "1", "1", NA),
  QID3       = c("2", "1", "3", NA, NA),
  stringsAsFactors = FALSE
)
rb <- realised_burden(qsf_path, responses)
rb[, c("response_id", "finished", "realised_points", "predicted_points")]
}

}
